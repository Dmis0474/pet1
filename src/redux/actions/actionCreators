import actionNames from "../actions/actionNames";

export const handleSubmit = (text) => {
  return (dispatch) => {
    return dispatch({
      type: actionNames.ADD,
      text: text,
    });
  };
};